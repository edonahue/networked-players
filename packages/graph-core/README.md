# Graph core

Planned responsibility: build and query the evidence-bearing music-credit graph.

The correctness model begins with an artist–release bipartite graph so a path retains the release that justifies each connection. Small readable fixtures should serve as the correctness oracle. Compact arrays, memory mapping, optimized libraries, or graph databases must be selected through measured comparison rather than assumption.
