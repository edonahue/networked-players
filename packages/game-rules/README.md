# Game rules

Planned responsibility: define challenges, answer validation, scoring, hints, role constraints, and explainable path presentation independently from a specific web framework.

The first game mode should be understandable from a static artifact and should not require arbitrary live graph search.
