# Minimal Ansible defaults for the example inventory. Copy the example inventory
# to a local, git-ignored one (inventories/local/) and point at it for real runs.
[defaults]
inventory = inventories/example/hosts.yml
retry_files_enabled = False
interpreter_python = auto_silent
stdout_callback = default
host_key_checking = True
