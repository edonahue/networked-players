# API

This directory is reserved for a future bounded search and challenge API.

No API is implemented or exposed yet. Before implementation, define request limits, snapshot version behavior, evidence response shape, caching, rate limiting, observability, and safe degradation.

The API must never expose collection membership, internal infrastructure identity, or unrestricted graph traversal.
